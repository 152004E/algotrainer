export default function TrainerToolsSidebar() {
  return (
    <aside className="w-16 border-l border-slate-200 dark:border-slate-800 flex flex-col items-center py-8 gap-6">

      <button className="text-gray-400 hover:text-blue-500">
        ?
      </button>

      <button className="text-gray-400 hover:text-blue-500">
        ⌨
      </button>

    </aside>
  );
}